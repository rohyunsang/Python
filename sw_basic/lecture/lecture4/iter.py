# Iterator - 반복자
# Iterable Object : 반복 가능 객체 
# 값을 하나씩 차례대로 반환 가능한 객체를 말하며, sequence, set, dictionary등이 있다.
# 순차적으로 다음 데이터를 리턴할 수 있는 객체
# 내장 함수 next를 사용하여 순환하는 다음 값을 반환함.
# iter() 내장 함수 : iterator 객체를 생성 할 수 있음
# next() 내장 함수 : iterator의 다음 값 반환

